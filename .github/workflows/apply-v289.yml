name: Apply v2.89 CPBL kind isolation

on:
  push:
    paths:
      - '.github/workflows/apply-v289.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Apply patch
        run: python .github/scripts/apply_v289.py
      - name: Syntax checks
        run: |
          node --check app.js
          node --check cpbl-realtime.js
          node --check postseason-history.js
      - name: Commit release
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app.js cpbl-realtime.js index.html service-worker.js version.json
          git commit -m "Release v2.89 isolate CPBL A/D realtime cache" || exit 0
          git push
