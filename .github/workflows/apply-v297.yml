name: Apply v2.97 NPB pitcher batting fixes

on:
  push:
    paths:
      - '.github/workflows/apply-v297.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Apply v2.97 patch
        run: python .github/scripts/apply_v297.py
      - name: Syntax checks
        run: |
          node --check app.js
          node --check cpbl-realtime.js
          node --check npb-realtime.js
          node --check game-detail-enhancement.js
          node --check landscape-state.js
          node --check postseason-history.js
          node --check .github/scripts/smoke_v297.mjs
      - name: Verify release wiring
        run: |
          grep -q '"version":"v2.97"' version.json
          grep -q "\['CPBL','NPB'\].*data?.league" landscape-state.js
          test ! -e .github/scripts/apply_v295.py
          test ! -e .github/scripts/smoke_v295.mjs
          test ! -e .github/workflows/apply-v295.yml
          test -e .github/scripts/apply_v296.py
          test -e .github/scripts/smoke_v296.mjs
          test -e .github/workflows/apply-v296.yml
      - name: Production integration smoke
        run: node .github/scripts/smoke_v297.mjs
      - name: Commit release
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Release v2.97 harden NPB pitcher batting and landscape refresh" || exit 0
          git push
